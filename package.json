{
  "name": "ai-toolkit-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev . --host 0.0.0.0",
    "sync:changelog": "node scripts/sync-changelog.mjs",
    "build": "vitepress build .",
    "preview": "vitepress preview . --host 0.0.0.0"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
